# PRD-0035: Multi-AI 끝장토론 Verifier

**Version**: 3.0.0 | **Date**: 2026-01-18 | **Status**: Draft
**Priority**: P1 | **Type**: Enhancement

---

## 1. Executive Summary

### 핵심 컨셉: "끝장 토론"

**3개 AI가 100% 합의에 도달할 때까지 토론을 계속**합니다. 다수결이 아닌 **완전 합의**가 목표입니다.

```
┌─────────────────────────────────────────────────────────────────────┐
│                      🔥 끝장 토론 (Ultimate Debate)                  │
│                                                                      │
│   "모든 AI가 동의할 때까지 끝나지 않는다"                             │
│                                                                      │
│   ┌─────────┐    ┌─────────┐    ┌─────────┐                         │
│   │ Claude  │◄──►│ Gemini  │◄──►│  GPT    │                         │
│   │ Opus4.5 │    │ 3 Pro   │    │  5.2    │                         │
│   └────┬────┘    └────┬────┘    └────┬────┘                         │
│        │              │              │                               │
│        └──────────────┼──────────────┘                               │
│                       │                                              │
│                       ▼                                              │
│              ┌────────────────┐                                      │
│              │ 합의 도달?     │                                      │
│              └────────┬───────┘                                      │
│                       │                                              │
│            ┌──────────┴──────────┐                                   │
│            │                     │                                   │
│         [NO]                  [YES]                                  │
│            │                     │                                   │
│            ▼                     ▼                                   │
│   ┌────────────────┐    ┌────────────────┐                          │
│   │ 다음 라운드    │    │ 🎯 끝장토론    │                          │
│   │ 계속 진행      │    │    종료!       │                          │
│   └────────────────┘    └────────────────┘                          │
└─────────────────────────────────────────────────────────────────────┘
```

### Context 최적화 전략

| 문제 | 해결책 |
|------|--------|
| 토론 내용이 Main Context 소비 | **모든 라운드를 MD 파일로 저장** |
| 반복 호출로 비용 증가 | **변경 없는 결론은 캐시** |
| 동일 작업 중복 수행 | **작업 ID + 해시로 중복 감지** |

---

## 2. 끝장토론 프로토콜

### 2.1 동작 흐름

```
/auto "API 리팩토링"
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│  Phase 1: 병렬 1차 분석 (3 AI 동시)                            │
│  ─────────────────────────────────────────                    │
│  Claude, Gemini, GPT가 동일한 작업을 독립 분석                 │
│  결과 → .claude/debates/{task_id}/round_01/ 에 저장           │
└───────────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│  Phase 2: 교차 검토                                            │
│  ─────────────────────────────────────────                    │
│  각 AI가 다른 2개 AI의 분석을 검토                             │
│  동의 / 반박 / 보완 의견 제시                                  │
│  결과 → .claude/debates/{task_id}/cross_review/ 에 저장       │
└───────────────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│  Phase 3: 합의 체크                                            │
│  ─────────────────────────────────────────                    │
│  3개 AI의 결론이 동일한가?                                     │
│                                                               │
│  [YES] → Phase 5 (종료)                                       │
│  [NO]  → Phase 4 (재토론)                                     │
└───────────────────────────────────────────────────────────────┘
        │
        ▼ (합의 실패 시)
┌───────────────────────────────────────────────────────────────┐
│  Phase 4: 재토론 (최대 5라운드)                                │
│  ─────────────────────────────────────────                    │
│  반박 포인트만 집중 토론                                       │
│  이전 라운드 MD 파일 참조하여 맥락 유지                        │
│  결과 → .claude/debates/{task_id}/round_0N/ 에 저장           │
│                                                               │
│  → Phase 3으로 돌아가 합의 재확인                              │
└───────────────────────────────────────────────────────────────┘
        │
        ▼ (합의 도달 또는 5라운드 초과)
┌───────────────────────────────────────────────────────────────┐
│  Phase 5: 최종 결과                                            │
│  ─────────────────────────────────────────                    │
│  ✅ 합의된 전략 → 자동 실행                                    │
│  ⚠️ 미합의 항목 → 사용자 판단 요청                             │
│  결과 → .claude/debates/{task_id}/FINAL.md 에 저장            │
└───────────────────────────────────────────────────────────────┘
```

### 2.2 파일 구조 (Context 절약 핵심)

```
.claude/debates/
├── {task_id}/                          # 작업별 폴더
│   ├── TASK.md                         # 원본 작업 정의
│   ├── round_01/
│   │   ├── claude.md                   # Claude 1차 분석
│   │   ├── gemini.md                   # Gemini 1차 분석
│   │   └── gpt.md                      # GPT 1차 분석
│   ├── cross_review/
│   │   ├── claude_reviews_gemini.md
│   │   ├── claude_reviews_gpt.md
│   │   ├── gemini_reviews_claude.md
│   │   ├── gemini_reviews_gpt.md
│   │   ├── gpt_reviews_claude.md
│   │   └── gpt_reviews_gemini.md
│   ├── round_02/                       # 재토론 (필요시)
│   │   └── ...
│   ├── CONSENSUS.md                    # 합의 상태 추적
│   └── FINAL.md                        # 최종 결과
└── index.yaml                          # 전체 토론 인덱스
```

### 2.3 MD 파일 형식

#### 1차 분석 파일 (round_01/claude.md)
```markdown
# Claude Opus 4.5 분석 결과

**Task ID**: api-refactoring-20260118-001
**Round**: 1
**Timestamp**: 2026-01-18T10:00:00Z

## 전략 제안

### 1. 검토 결과
- API 엔드포인트 15개 분석 완료
- 주요 이슈: 인증 미들웨어 중복

### 2. 구현 전략
1. 인증 미들웨어 통합 (auth_middleware.py)
2. 엔드포인트별 권한 데코레이터 적용
3. 테스트 커버리지 80% 이상 확보

### 3. 예상 변경 파일
| 파일 | 변경 유형 |
|------|----------|
| auth_middleware.py | 신규 생성 |
| api/users.py | 수정 |
| api/products.py | 수정 |

### 4. 결론 해시
`SHA256: abc123...` (동일 결론 비교용)
```

#### 교차 검토 파일 (cross_review/claude_reviews_gemini.md)
```markdown
# Claude → Gemini 분석 검토

**Reviewing**: gemini.md (Round 1)
**Reviewer**: Claude Opus 4.5

## 동의 항목 ✅
- [x] 인증 미들웨어 통합 필요성
- [x] 테스트 커버리지 목표 80%

## 반박 항목 ❌
- [ ] 데코레이터 대신 미들웨어 체인 제안
  - **근거**: 성능 오버헤드 우려
  - **대안**: 미들웨어 체인 + 캐싱

## 보완 제안 ➕
- 로깅 미들웨어 추가 권장
- 에러 핸들링 통일 필요

## 합의 가능 여부
**[PARTIAL]** - 1개 항목 반박 존재
```

---

## 3. Context 최적화 기법

### 3.1 Main Context 보존 전략

```python
class DebateContextManager:
    """Context 최소화 전략"""

    def __init__(self, task_id: str):
        self.task_id = task_id
        self.base_path = f".claude/debates/{task_id}"

    async def run_parallel_analysis(self, task: str) -> None:
        """
        1차 분석: 3개 AI 병렬 실행
        - Main Context에는 요약만 저장
        - 전체 분석은 MD 파일로
        """
        results = await asyncio.gather(
            self._call_claude(task),
            self._call_gemini(task),
            self._call_gpt(task)
        )

        # MD 파일로 저장 (Context 절약)
        for model, result in zip(["claude", "gemini", "gpt"], results):
            self._save_to_md(f"round_01/{model}.md", result)

        # Main Context에는 요약만
        return {
            "status": "round_01_complete",
            "files": [f"round_01/{m}.md" for m in ["claude", "gemini", "gpt"]]
        }

    def _save_to_md(self, path: str, content: dict) -> None:
        """MD 파일로 저장"""
        full_path = f"{self.base_path}/{path}"
        # ... 파일 저장 로직
```

### 3.2 중복 작업 방지

```python
class DuplicateDetector:
    """동일 작업 중복 수행 방지"""

    def __init__(self):
        self.cache_path = ".claude/debates/cache.json"

    def get_task_hash(self, task: str, context: str) -> str:
        """작업 + 컨텍스트 해시 생성"""
        return hashlib.sha256(f"{task}:{context}".encode()).hexdigest()[:16]

    def is_duplicate(self, task_hash: str) -> bool:
        """이전에 동일 작업 수행했는지 확인"""
        cache = self._load_cache()
        return task_hash in cache

    def get_cached_result(self, task_hash: str) -> dict | None:
        """캐시된 결과 반환"""
        cache = self._load_cache()
        if task_hash in cache:
            result_path = cache[task_hash]["final_path"]
            return self._load_md(result_path)
        return None
```

### 3.3 충돌 방지 전략

```yaml
# .claude/debates/locks.yaml
locks:
  - task_id: "api-refactoring-001"
    locked_by: "claude"
    locked_at: "2026-01-18T10:00:00Z"
    status: "in_progress"

  # 다른 AI가 같은 작업 시작하려 하면 대기
```

---

## 4. /auto 통합

### 4.1 자동 끝장토론 트리거

```python
# auto_executor.py 수정

class AutoExecutor:
    async def execute(self, task: str) -> dict:
        """
        모든 /auto 작업에서 자동으로 끝장토론 실행
        """
        # 1. 끝장토론 시작
        debate = UltimateDebate(task)

        # 2. Phase 1: 병렬 1차 분석
        await debate.run_parallel_analysis()

        # 3. Phase 2: 교차 검토
        await debate.run_cross_review()

        # 4. Phase 3-4: 합의 도달까지 반복
        while not debate.is_consensus_reached():
            if debate.round > 5:
                break  # 최대 5라운드
            await debate.run_debate_round()

        # 5. Phase 5: 최종 결과 → 자동 실행
        final_strategy = debate.get_final_strategy()

        if final_strategy.is_fully_agreed:
            return await self._execute_strategy(final_strategy)
        else:
            return await self._request_user_decision(final_strategy)
```

### 4.2 /auto 커맨드 플로우

```
/auto "API 리팩토링"
        │
        ▼
┌────────────────────────────────────┐
│ 🔥 끝장토론 자동 시작              │
│                                    │
│ Round 1: 3 AI 병렬 분석...         │
│ Round 2: 교차 검토...              │
│ Round 3: 재토론 (반박 포인트)...   │
│ Round 4: 최종 합의 도달! ✅        │
└────────────────────────────────────┘
        │
        ▼
┌────────────────────────────────────┐
│ 📋 최종 전략 (100% 합의)           │
│                                    │
│ 1. auth_middleware.py 생성         │
│ 2. 엔드포인트 리팩토링              │
│ 3. 테스트 추가                      │
│                                    │
│ → 자동 실행 시작...                │
└────────────────────────────────────┘
        │
        ▼
┌────────────────────────────────────┐
│ ✅ 작업 완료                        │
│                                    │
│ 변경: 5개 파일, +250, -80          │
│ 테스트: 15/15 통과                 │
│ 토론 로그: debates/api-001/        │
└────────────────────────────────────┘
```

---

## 5. 합의 판정 로직

### 5.1 합의 기준

```python
class ConsensusChecker:
    """합의 도달 여부 판정"""

    def check_consensus(self, analyses: list[dict]) -> ConsensusResult:
        """
        합의 조건:
        1. 핵심 전략 동일 (파일 변경 목록)
        2. 구현 순서 동일
        3. 반박 항목 0개
        """
        # 결론 해시 비교
        hashes = [a["conclusion_hash"] for a in analyses]

        if len(set(hashes)) == 1:
            return ConsensusResult(
                status="FULL_CONSENSUS",
                agreed_items=analyses[0]["strategy"],
                disputed_items=[]
            )

        # 부분 합의 체크
        common_items = self._find_common_items(analyses)
        disputed_items = self._find_disputed_items(analyses)

        if not disputed_items:
            return ConsensusResult(
                status="FULL_CONSENSUS",
                agreed_items=common_items,
                disputed_items=[]
            )

        return ConsensusResult(
            status="PARTIAL_CONSENSUS",
            agreed_items=common_items,
            disputed_items=disputed_items,
            next_action="CONTINUE_DEBATE"
        )
```

### 5.2 재토론 포커싱

```python
async def run_focused_debate(self, disputed_items: list[dict]) -> None:
    """
    반박 포인트만 집중 토론
    - 전체 재분석 X
    - 불일치 항목만 논의
    """
    prompt = f"""
    이전 라운드에서 다음 항목에 대해 의견이 갈렸습니다:

    ## 논쟁 포인트
    {self._format_disputed_items(disputed_items)}

    ## 다른 AI들의 주장
    {self._load_other_opinions(disputed_items)}

    ## 지시
    1. 각 논쟁 포인트에 대해 입장을 명확히 하세요
    2. 다른 AI의 주장에 동의한다면 그렇게 밝히세요
    3. 여전히 반대한다면 추가 근거를 제시하세요

    ## 응답 형식
    - 동의: [항목 ID] - 수용
    - 유지: [항목 ID] - [추가 근거]
    """

    # 3 AI에게 포커스된 토론 요청
    await asyncio.gather(
        self._claude_focused_debate(prompt),
        self._gemini_focused_debate(prompt),
        self._gpt_focused_debate(prompt)
    )
```

---

## 6. 구현 사양

### 6.1 디렉토리 구조

```
.claude/skills/ultimate-debate/
├── SKILL.md
├── scripts/
│   ├── __init__.py
│   ├── main.py                    # CLI 엔트리포인트
│   ├── debate/
│   │   ├── __init__.py
│   │   ├── orchestrator.py        # 토론 오케스트레이터
│   │   ├── consensus_checker.py   # 합의 판정
│   │   ├── duplicate_detector.py  # 중복 방지
│   │   └── context_manager.py     # MD 파일 관리
│   ├── models/
│   │   ├── __init__.py
│   │   ├── claude_client.py       # Claude API
│   │   ├── gemini_client.py       # Gemini API
│   │   └── gpt_client.py          # GPT API
│   └── prompts/
│       ├── analysis_prompt.py
│       ├── review_prompt.py
│       └── debate_prompt.py
└── tests/
    └── test_debate.py
```

### 6.2 /auto 커맨드 통합

```markdown
# .claude/commands/auto.md 수정사항

## 🔥 끝장토론 모드 (자동 활성화)

모든 /auto 작업에서 자동으로 Multi-AI 끝장토론이 실행됩니다.

### 동작 방식
1. 3개 AI (Claude/Gemini/GPT)가 동일 작업 병렬 분석
2. 교차 검토 → 합의 체크
3. 100% 합의까지 재토론 (최대 5라운드)
4. 합의된 전략 자동 실행

### 비활성화
```bash
/auto "작업" --no-debate    # 끝장토론 건너뛰기
```

### 토론 로그 확인
```bash
/auto debate-log            # 최근 토론 로그
/auto debate-log {task_id}  # 특정 토론 상세
```
```

---

## 7. 예상 효과

| 지표 | 기존 (다수결) | 끝장토론 (100% 합의) |
|------|:------------:|:------------------:|
| 전략 정확도 | ~85% | **~98%** |
| 실행 후 수정률 | ~20% | **~3%** |
| Context 소비 | 15% | **8%** (MD 저장) |
| 평균 라운드 수 | 3 (고정) | 2.5 (가변) |

---

## 8. 체크리스트

### 구현 체크리스트

- [ ] ultimate-debate 스킬 생성
- [ ] debate/orchestrator.py 구현
- [ ] consensus_checker.py 구현
- [ ] duplicate_detector.py 구현
- [ ] context_manager.py (MD 저장) 구현
- [ ] Claude/Gemini/GPT 클라이언트 구현
- [ ] /auto 커맨드 통합
- [ ] 토론 로그 뷰어 구현
- [ ] 테스트 작성

### 검증 체크리스트

- [ ] 3 AI 합의 도달 테스트
- [ ] 5라운드 내 미합의 처리 테스트
- [ ] Context 소비량 측정
- [ ] 중복 작업 방지 테스트
- [ ] MD 파일 저장/로드 테스트

---

## 9. 참조

- PRD-0035 v2.0 (Debate-Based Consensus)
- [Multi-Agent Debate Framework](https://www.emergentmind.com/topics/multiagent-debate-framework)
- [Diverse Multi-Agent Debate](https://openreview.net/forum?id=t6QHYUOQL7)
