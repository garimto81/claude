# 챗봇 서버
PORT=3002
HOST=localhost

# Ollama 설정
OLLAMA_BASE_URL=http://localhost:11434
OLLAMA_MODEL=qwen3:8b

# ==========================================
# YouTube OAuth 2.0 설정 (필수)
# ==========================================
# Google Cloud Console에서 OAuth 2.0 클라이언트 ID 생성:
# 1. https://console.cloud.google.com 접속
# 2. API 및 서비스 → 사용자 인증 정보
# 3. OAuth 2.0 클라이언트 ID 생성 (웹 애플리케이션)
# 4. 승인된 리디렉션 URI에 http://localhost:3002/oauth/callback 추가
GOOGLE_CLIENT_ID=                    # OAuth 클라이언트 ID
GOOGLE_CLIENT_SECRET=                # OAuth 클라이언트 시크릿
GOOGLE_REDIRECT_URI=http://localhost:3002/oauth/callback

# YouTube 자동 감지 (선택)
# YOUTUBE_API_KEY=                    # Google Cloud Console에서 발급
# YOUTUBE_CHANNEL_ID=                 # 채널 ID (UC로 시작)

# 챗봇 설정
BOT_NAME=CodingBot
RESPONSE_DELAY_MS=500
MAX_RESPONSE_LENGTH=200
ENABLE_AUTO_GREETING=true

# GitHub 동기화 설정 (선택)
GITHUB_TOKEN=                    # Personal Access Token (레포 조회용)
GITHUB_AUTO_SYNC=false           # 앱 시작 시 자동 동기화 여부
GITHUB_ACTIVITY_DAYS=5           # 최근 활동 조회 기간 (일)
