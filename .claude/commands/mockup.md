---
name: mockup
description: HTML 와이어프레임 목업 생성 + 스크린샷 자동화
---

# /mockup - 와이어프레임 목업 생성

HTML 와이어프레임을 생성하고 Playwright로 스크린샷을 캡처합니다.

## Usage

```bash
/mockup [name] [options]

Options:
  --style=TYPE      wireframe (기본) | detailed
  --bnw             Black & White T&M 모드 (흑백 + 플레이스홀더)
  --screens=N       생성할 화면 수 (1-5, 기본: 1)
  --prd=PRD-NNNN    연결할 PRD 번호 (자동 삽입)
  --flow            전체 흐름 다이어그램 포함
```

## 예시

```bash
/mockup "로그인 화면"                    # 단일 화면
/mockup "대시보드" --bnw                 # B&W T&M 와이어프레임
/mockup "대시보드" --screens=3           # 3개 화면
/mockup "인증 흐름" --flow --prd=PRD-0003  # 흐름 + PRD 연결
```

---

## 워크플로우

```
/mockup [name]
      │
      ▼
┌─────────────────────────────────┐
│ 1. 질문 (필요시)                │
│    - 화면 요소 (폼/목록/카드)   │
│    - 레이아웃 (1열/2열/사이드)  │
└─────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────┐
│ 2. HTML 와이어프레임 생성       │
│    docs/mockups/{name}.html     │
│    (와이어프레임 템플릿 사용)   │
└─────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────┐
│ 3. Playwright 스크린샷          │
│    npx playwright screenshot    │
│    docs/images/{name}.png       │
└─────────────────────────────────┘
      │
      ▼
┌─────────────────────────────────┐
│ 4. 결과 출력                    │
│    - HTML 경로                  │
│    - 이미지 경로                │
│    - PRD 삽입 코드 (옵션)       │
└─────────────────────────────────┘
```

---

## 화면 요소

### 선택 가능 요소

| 요소 | 설명 | 예시 |
|------|------|------|
| `header` | 상단 네비게이션 | 로고, 메뉴, 사용자 |
| `sidebar` | 좌측 사이드바 | 메뉴, 필터 |
| `form` | 입력 폼 | 로그인, 회원가입 |
| `table` | 데이터 테이블 | 목록, 관리 화면 |
| `cards` | 카드 그리드 | 대시보드, 갤러리 |
| `modal` | 팝업 모달 | 확인, 상세보기 |

### 레이아웃 옵션

```
┌─────────────────────┐   ┌───────┬─────────────┐   ┌─────────────────────┐
│      1-Column       │   │ Side  │   Content   │   │  Header             │
│      (Default)      │   │ bar   │             │   ├──────────┬──────────┤
│                     │   │       │             │   │  Left    │  Right   │
└─────────────────────┘   └───────┴─────────────┘   └──────────┴──────────┘
```

---

## 출력 예시

```bash
/mockup "로그인 화면" --flow

# Output:
# ┌────────────────────────────────────────────┐
# │  Mockup: 로그인 화면                       │
# ├────────────────────────────────────────────┤
# │  화면 요소: header, form                   │
# │  레이아웃: 1-column, centered              │
# └────────────────────────────────────────────┘
#
# ✓ HTML 생성: docs/mockups/login.html
# ✓ 스크린샷: docs/images/login.png
#
# 📋 Markdown 삽입 코드:
# ![로그인 화면](../images/login.png)
# [HTML 원본](../mockups/login.html)
```

---

## 스타일 비교

### wireframe (기본)

```
┌─────────────────────────────────┐
│  [Logo]            [Menu] [User]│
├─────────────────────────────────┤
│                                 │
│     ┌─────────────────────┐     │
│     │  Email              │     │
│     └─────────────────────┘     │
│     ┌─────────────────────┐     │
│     │  Password           │     │
│     └─────────────────────┘     │
│     [      Login      ]         │
│                                 │
└─────────────────────────────────┘
```

- 흑백 박스 레이아웃
- 빠른 생성 (5초)
- 구조 중심

### --bnw (Black & White T&M)

**B**lack & **W**hite + **T**ext/**M**edia 플레이스홀더 전용 모드

| 요소 | 표현 |
|------|------|
| 색상 | `#000`, `#fff`, `#ccc` 만 사용 |
| 텍스트 | `Lorem ipsum`, `[Title]`, `[Label]` |
| 이미지 | `[Image]`, `[Photo]`, `[Banner]` |
| 아이콘 | `[Icon]`, `[▶]`, `[✕]` |
| 로고 | `[Logo]`, `[Brand]` |

```bash
# 단축 호출
/auto --mockup --bnw "화면명"

# 직접 호출
/mockup "화면명" --bnw
```

> **용도**: 구조 검토, 빠른 프로토타이핑, 레이아웃 논의

### detailed

- 컬러, 그림자, 아이콘
- 실제 UI와 유사
- 생성 시간 긴 편 (15초)

---

## 흐름 다이어그램 (--flow)

```
┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐
│  Start  │────▶│  Login  │────▶│ Verify  │────▶│Dashboard│
└─────────┘     └─────────┘     └─────────┘     └─────────┘
                     │
                     ▼
               ┌─────────┐
               │  Error  │
               └─────────┘
```

`--flow` 옵션 사용 시 화면 간 연결 흐름도 함께 생성됩니다.

---

## PRD 연결 (--prd)

```bash
/mockup "인증 화면" --prd=PRD-0003

# 자동 수행:
# 1. docs/mockups/PRD-0003/auth.html 생성
# 2. docs/images/PRD-0003/auth.png 캡처
# 3. PRD-0003 문서에 시각화 섹션 추가
```

---

## 관련 커맨드

| 커맨드 | 용도 |
|--------|------|
| `/create prd --visualize` | PRD 생성 시 목업 포함 |
| `/create docs` | API/코드 문서 생성 |

---

## 템플릿 경로

- 와이어프레임: `.claude/templates/mockup-wireframe.html`
- 상세: `.claude/templates/mockup-detailed.html`
- 흐름도: `.claude/templates/mockup-flow.html`
