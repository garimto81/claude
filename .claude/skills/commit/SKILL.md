---
name: commit
description: Conventional Commit 형식으로 git 커밋 생성 및 push
triggers:
  keywords:
    - "commit"
    - "커밋"
    - "git commit"
---

# /commit - Conventional Commit & Push

이 스킬은 `.claude/commands/commit.md` 커맨드 파일의 내용을 실행합니다.

## 사용법

```bash
/commit              # 커밋 및 push
/commit --no-push    # 커밋만, push 생략
```

## 커맨드 파일 참조

상세 워크플로우: `.claude/commands/commit.md`
